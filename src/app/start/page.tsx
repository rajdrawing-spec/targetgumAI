import { TryItOnboarding } from '@/components/tryit/onboarding'

/** Public try-it, step 1: tell Gummy about your business (Growth Map node 1). */
export default function StartPage() {
  return <TryItOnboarding />
}
