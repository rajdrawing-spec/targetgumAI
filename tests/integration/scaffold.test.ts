import { describe, it } from 'vitest'

// Placeholder. Real integration tests (database, Claude, Metricool, GA4,
// GSC, Canva) land alongside each integration - see docs/MVP-CHECKLIST.md.
describe.skip('integration placeholder', () => {
  it('todo', () => {})
})
