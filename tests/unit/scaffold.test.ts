import { describe, expect, it } from 'vitest'

// Placeholder test proving the test runner, tsconfig path alias, and CI
// pipeline are wired correctly. Replace/extend once real modules land
// (see docs/MVP-CHECKLIST.md).
describe('scaffold', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2)
  })
})
