import { describe, it } from 'vitest'

// Placeholder. The adversarial security suite (cross-client access,
// privilege escalation, tool authorization, prompt injection, secret
// leakage - see docs/BRD-PRD.md Section 80 and docs/SECURITY.md) lands
// with the auth/RBAC foundation and again with the AI Gateway.
describe.skip('security placeholder', () => {
  it('todo', () => {})
})
