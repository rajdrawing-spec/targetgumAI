# External Approval Tracker — TargetGum AI Marketing OS

Per BRD-PRD Section 90. External approval timelines are dependencies, not guaranteed
delivery dates (Section 91) — development proceeds against mock providers wherever an
item below is not yet `CONNECTED`.

| Provider | Application | Required Credentials | Status | Submitted | Expected Window | Owner | Blocker | Next Action |
|---|---|---|---|---|---|---|---|---|
| Claude / Anthropic | API access | `ANTHROPIC_API_KEY` | **STILL NEEDED** | — | Immediate (self-serve) | TargetGum | AI Gateway (Day 4) is built and its orchestration logic (retries, ai_runs persistence, structured-output validation) is verified against a mocked client, but no live call against the real API has been made — no key configured in this environment | User provides API key so a live smoke test can run and Day 9+ (real agents) can be verified end-to-end |
| Metricool | Account + API/MCP access | `METRICOOL_MCP_URL` + `METRICOOL_API_KEY` for the deployed app (separate from this dev session's own MCP connection) | **ADAPTER BUILT AND LIVE-DATA-VERIFIED, NOT LIVE-CONNECTED FROM THE APP** | — | — | TargetGum | The MetricoolProvider adapter (Day 6) is built against verified tool schemas; on Day 15, its analytics/campaign parsing was additionally verified against *real returned data* (checked live via this session's own Metricool MCP connection, account `info@tapashub.com`, across 5 brands: HUGFAB, LHO, TargetGum, undertreegames, Pepalworks) — this caught and fixed a real bug where every metric silently came back empty (see `docs/INTEGRATIONS.md`). But the **deployed application** still has no `METRICOOL_MCP_URL`/`METRICOOL_API_KEY` of its own — this session's MCP connection isn't reachable from the app's own runtime, so end-to-end connectivity *from the app* is still unverified. Ads *write/management* is confirmed unavailable via Metricool regardless (capability gap in the MCP server itself, not account-plan). Only LHO and undertreegames have a connected ads account today (Facebook/Meta Ads; no Google Ads connected on any brand) - the `campaigns` connector's response shape is inferred from the verified `evolution` connector, not independently confirmed. See `docs/DECISIONS.md` for full findings. | User provides a real `METRICOOL_MCP_URL` + `METRICOOL_API_KEY` for the app to connect with, and confirms which brand is the pilot client, so a live smoke test can run (like the Day 3 auth one) and the `campaigns` connector shape can be confirmed against a populated ads account |
| GA4 | OAuth app + property access | `GA4_OAUTH_CLIENT_ID`/`SECRET` + a pilot property ID | **ADAPTER BUILT, NOT LIVE-CONNECTED** | — | — | TargetGum | The GA4 adapter (Day 7) is built against the official `googleapis` client's own type definitions (not guessed) and passes its full test suite via the mock provider, but no real Google Cloud OAuth app has ever been configured, so neither the OAuth flow nor the Data API call has been exercised against real Google infrastructure | User creates a Google Cloud OAuth client (Analytics readonly scope), provides `GA4_OAUTH_CLIENT_ID`/`SECRET`, and identifies the pilot client's GA4 property so a live smoke test can run |
| Google Search Console | OAuth app + property access | `GSC_OAUTH_CLIENT_ID`/`SECRET` (can reuse the same Google Cloud OAuth client as GA4, different scope) + a verified pilot site | **ADAPTER BUILT, NOT LIVE-CONNECTED** | — | — | TargetGum | Same situation as GA4 above - adapter built and tested via mock, OAuth app not yet real | User provides `GSC_OAUTH_CLIENT_ID`/`SECRET` and confirms the pilot client's verified Search Console property |
| Canva | Developer/MCP access | Canva Developer app, OAuth client | **ADAPTER STUBBED (UnsupportedOperationError), MOCK BUILT AND TESTED** | — | Uncertain | TargetGum | The creative workflow (Phase 2, Day 17) was built and fully tested against `CanvaMockProvider`. The real adapter (`createCanvaProvider`) throws `UnsupportedOperationError` for every method - no Canva MCP connection has ever been available in this environment to verify tool names/schemas against. See docs/DECISIONS.md | User provides a Canva Developer app/MCP connection details, so the real adapter's tool names/schemas can be confirmed and implemented against real Canva infrastructure |
| Google Ads | Native API access | Developer token, Google Cloud project, OAuth, test accounts | **ADAPTER STUBBED (UnsupportedOperationError), MOCK BUILT AND TESTED** | — | Phase 2 | TargetGum | Metricool proved insufficient for ads management (no write endpoints at all - see docs/INTEGRATIONS.md), so the native `AdsProvider` implementation (Phase 2, Day 16) was built against `GoogleAdsMockProvider`. The real adapter (`createGoogleAdsProvider`) throws `UnsupportedOperationError` for every method - there is no official Node.js client for the Google Ads API at all (Google's own client-library list covers Java/.NET/PHP/Python/Perl/Ruby, not Node), so nothing was guessed against an unverifiable protocol. See docs/DECISIONS.md | User provides a developer token, a Google Cloud project with the Ads API enabled, an OAuth client, and a test ad account, so the real adapter can actually be written and verified against real Google Ads infrastructure |
| Meta | Native API access | Meta developer app, OAuth, app review | **ADAPTER STUBBED (UnsupportedOperationError), MOCK BUILT AND TESTED** | — | Phase 2 | TargetGum | Same trigger as Google Ads above. The real adapter (`createMetaAdsProvider`) throws `UnsupportedOperationError` for every method - Meta does publish an official Node SDK (`facebook-nodejs-business-sdk`), but it's deliberately not added as a dependency: nothing in this environment (no developer app, no app review, no test ad account) can verify calls written against it. See docs/DECISIONS.md | User creates a Meta developer app, completes the relevant app review, and provides a test ad account, so the real adapter can be written against `facebook-nodejs-business-sdk` and verified |
| Amazon Ads | Native API access | Amazon Ads API access application | **NOT NEEDED FOR MVP** | — | Phase 3 | TargetGum | — | Defer |

Update this table as each item's status changes. "PARTIAL" means a connection exists
but scope/suitability for production is unconfirmed — treat it the same as "NEEDED"
for anything beyond mock-provider development until confirmed.
