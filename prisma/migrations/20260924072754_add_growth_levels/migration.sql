-- CreateTable
CREATE TABLE "growth_levels" (
    "level" INTEGER NOT NULL,
    "title" TEXT NOT NULL,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "growth_levels_pkey" PRIMARY KEY ("level")
);
